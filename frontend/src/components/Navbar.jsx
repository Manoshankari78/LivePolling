import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function Navbar(){const {user,logout}=useAuth(),nav=useNavigate();return <header className="nav"><Link className="brand" to="/"><span className="brand-mark">✦</span> PulsePoll</Link><nav>{user?<><Link to="/dashboard">Dashboard</Link><button className="link-button" onClick={()=>{logout();nav('/')}}>Logout</button></>:<><Link to="/login">Login</Link><Link className="nav-cta" to="/register">Get started</Link></>}</nav></header>}
