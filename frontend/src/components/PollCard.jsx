import {Link} from 'react-router-dom';
export default function PollCard({poll,onDelete,onClose,onShare}){const total=poll.options.reduce((s,o)=>s+o.votes,0);return <article className="poll-card"><div className="poll-card-top"><span className={`status ${poll.status}`}>{poll.status}</span><span>{new Date(poll.createdAt).toLocaleDateString()}</span></div><h3>{poll.question}</h3><p>{poll.options.length} options · {total} votes</p><div className="card-actions"><Link className="button small" to={`/poll/${poll.id}`}>View</Link><Link className="button small secondary" to={`/poll/${poll.id}/edit`}>Edit</Link><button className="button small secondary" onClick={()=>onShare(poll.id)}>Share</button>{poll.status==='active'&&<button className="button small secondary" onClick={()=>onClose(poll.id)}>Close</button>}<button className="button small danger" onClick={()=>onDelete(poll.id)}>Delete</button></div></article>}
